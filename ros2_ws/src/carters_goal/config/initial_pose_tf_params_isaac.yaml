initial_pose_tf_publisher:
  ros__parameters:
    use_sim_time: true
    global_frame: map
    odom_frame: odom
    # These arrays are overwritten at launch time from the shared team config.
    robot_namespaces: []

    # Flattened [x, y, z, qx, qy, qz, qw] array, rewritten at launch time
    # from warehouse_team_config.yaml.
    initial_poses: []
    odom_topic_suffix: chassis/odom
    reset_alignment_timeout_sec: 0.5
